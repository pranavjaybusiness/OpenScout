"""
Backend service integrations, grouped by domain:

- ``comparison`` — cross-marketplace orchestration
- ``ebay`` — eBay Browse API + affiliate links
- ``google_shopping`` — multi-retailer search via SearchAPI Google Shopping
- ``shein`` — SHEIN KOC affiliate link wrapping only (no SHEIN search API; applied to SHEIN URLs from Google Shopping)
- ``llm`` — Gemini extraction and verification
- ``marketplace`` — shared search queries, SearchAPI client, offer links
"""
